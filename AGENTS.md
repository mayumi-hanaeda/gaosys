# GAOSYS 開発グランドルール

重要:

今後のGAOSYS作業のメインリポジトリは `C:\GAOSYS２\gaosys-github` とする。
作業開始時は必ず本ファイル `C:\GAOSYS２\gaosys-github\AGENTS.md` を読むこと。
大元・大原則は本ファイル `C:\GAOSYS２\gaosys-github\AGENTS.md` を基準とする。
本ファイルこそが、共同編集者全員にとって唯一無二の共通ルールブックであり、GAOSYS作業の最優先グランドルールである。
他のルール、メモ、引き継ぎ文書、旧作業ディレクトリの内容と矛盾する場合は、ユーザーの明示指示がない限り本ファイルを優先する。

## 0. 正本・共同作業・履歴復旧

### 0.1 唯一の正本

- GitHub 上の `mayumi-hanaeda/gaosys` リポジトリの `main` を、GAOSYS の唯一の共有正本とする。
- 作業ディレクトリは `C:\GAOSYS２\gaosys-github` を使用する。旧ディレクトリ、退避コピー、ローカルだけのメモは正本ではない。
- 正本に含める仕様、運用手順、引継ぎ情報はこのリポジトリ内で管理する。実値を含む運用情報は `.local/` にのみ置く。

### 0.2 開発担当者の直接作業

- Pull Request を必須にはしない。開発担当者は `main` へ直接コミットしてよい。
- 通常作業ではブランチを作らない。同時に同じファイルまたは同じ機能領域を変更して競合する見込みがある場合、またはユーザーが指示した場合にだけブランチを作る。
- 各コミットは、復旧可能な一つの目的に絞り、変更理由と検証結果を追跡できるメッセージにする。大きな変更、削除、設定変更、本番操作の前後では必ずコミットまたは明確な差分を残す。
- 作業開始時は `git status` を確認する。未コミット変更やローカルが `main` より先行している状態を、他者の変更として無断で破棄・上書きしない。

### 0.3 競合と復旧

- 競合が発生したら、目的・競合ファイル・解決方針を確認してから解決する。意図を確認できない場合は停止してユーザーへ報告する。
- 公開済みの `main` の履歴を、通常作業で `push --force`、`reset --hard`、履歴改変で書き換えてはならない。問題のある反映は、原則として対象コミットを特定し `git revert` で取り消す。
- GitHub 上のコミット SHA を復旧点とする。障害時は、最初に対象コミット、影響範囲、復旧後の確認方法を記録してから復旧する。

### 0.4 文書の優先順位

1. `AGENTS.md`: 全員に適用するグランドルールと共同作業方針。
2. `DEPLOYMENT.md`: push・デプロイの手順と検証基準。
3. `RUNBOOK.md`: 障害時の復旧手順。
4. `NEXT_WORK_HANDOFF.md`: 次の作業で確認する現在の状態・未解決事項。
5. `SESSION_HANDOFF.md`: 経緯の保管用。現在の作業判断には使わない。

文書間で内容が食い違う場合は、上位の文書を優先し、実環境の状態は必ず再確認する。

このドキュメントは、本リポジトリで作業する AI コーディングアシスタント
（Codex）および開発者が必ず従う最上位ルールである。

本プロジェクトは銀行システムレベルのミッションクリティカルなシステムとして扱う。
機能要件、納期、利便性、作業効率を理由に、このルールから逸脱してはならない。

## 1. 最優先ルール: 機密情報とセキュリティの完全保護

この章は、他のすべてのルールに優先する。

### 1.1 機密情報の定義

以下はすべて機密情報として扱う。

- API キー
- OAuth token、refresh token、access token
- Webhook URL
- 秘密鍵、証明書、認証ファイル
- DB 接続情報
- 管理者 ID / パスワード
- 個人情報
- 顧客情報
- 業務データ
- セッション Cookie
- 本番環境の実 ID、URL、設定値のうち公開すべきでないもの

### 1.2 実値の記述・出力禁止

機密情報の実値を、以下に絶対に記述してはならない。

- コード
- コメント
- ログ
- テスト fixture
- ドキュメント
- チャット応答
- Pull Request
- Issue
- コミットメッセージ
- 実行結果の証跡

AI は、ユーザーが提示した秘密値を応答内で復唱、引用、再掲してはならない。
誤ってファイルへ書いた場合は、GitHub へ push する前に必ず取り消す。

### 1.3 外部送信の禁止

機密データ、業務データ、ソースコードを、ユーザーの明示的な承認なく以下へ送信してはならない。

- 外部サービス
- 別 LLM
- 外部 API
- 公開リポジトリ
- チャット、ログ、スクリーンショット共有先

### 1.4 安全な設定案内

秘密値が必要な場合は、キー名だけを案内する。
AI は秘密値そのものの入力を要求しない。

`.env.example` や設定例を作る場合は、以下のみを書く。

- キー名
- 説明
- ダミー値、例: `replace_me`

### 1.6 ローカル専用情報ファイル

作業継続に必要な実 ID、実 URL、本番 deployment 情報などは、ユーザーに何度も確認しないため
ローカル専用ファイルで管理してよい。

本リポジトリでは、以下をローカル専用情報ファイルとする。

```text
.local/OPERATIONS.md
```

このファイルと `.local/` 配下は、GitHub、Apps Script、外部サービスへ送信してはならない。
必ず `.gitignore` と `.claspignore` の両方で除外する。

ローカル専用情報ファイルには、以下を記録してよい。

- 本番 deployment ID
- 本番 version
- 本番 URL
- テスト用 deployment ID
- テスト用 URL
- Script Properties のキー名と設定状況
- 作業再開時に必要な確認済み運用情報

ただし、Webhook URL、token、パスワード、OAuth 認証情報などの高機密値は、
ローカル専用ファイルであっても原則として実値を書かない。

### 1.5 実行前確認

秘密値、個人情報、本番データを扱う可能性が少しでもある操作は、実行前に停止し、
目的、影響範囲、リスクを説明してユーザーの明示的な承認を得る。

## 2. 銀行レベルのコード品質と実装原則

生成、修正、レビューするコードは、監査可能で、堅牢で、保守性が高くなければならない。

### 2.1 厳格なエラーハンドリング

- 例外を握り潰してはならない。
- `catch (e) {}` のような空 catch を禁止する。
- エラー時は安全側に倒す。原則として fail-close を採用する。
- ユーザー向けエラーには、スタックトレース、内部構造、秘密値、個人情報を含めない。
- 内部ログにも秘密値、個人情報、生データを含めない。

### 2.2 ログ出力

重要フローでは、開始、成功、失敗を構造化ログで記録する。

対象例:

- 認証
- データ更新
- 外部通信
- Spreadsheet 書き込み
- Calendar / Meet 操作
- Chat / Mail 送信
- デプロイ、設定変更

ログには以下を含めてよい。

- 関数名
- 処理種別
- ステータス
- エラーコード
- `submissionId` など、個人情報でない相関 ID

ログに以下を含めてはならない。

- メールアドレス
- 氏名
- Webhook URL
- token
- API key
- 本文、自由記述、生データ

### 2.3 JSDoc / Docstring / コメント

すべての公開関数、重要な内部関数、モジュールには JSDoc または同等の説明を書く。

最低限、以下を記述する。

- 目的
- 引数の型と意味
- 戻り値
- 発生しうるエラー、または返却されるエラーコード

コメントは「何をしているか」だけでなく、「なぜその実装にしたか」を残す。
ただし、明白な処理への冗長なコメントは避ける。

### 2.4 DRY 原則

- 同一の処理、定数、文言、ロジックを重複させない。
- 新規追加前に、既存の共通関数、定数、モジュールを確認する。
- 本質的に異なる処理を無理に共通化して複雑化させてはならない。

### 2.5 ハードコーディング禁止

以下は原則としてコードへ直書きしない。

- URL
- ID
- 環境名
- 権限名
- 外部連携先
- マジックナンバー
- 変更されうる固定文言

本プロジェクトでは、実行時設定は原則として Script Properties に置く。
公開リポジトリへ送る可能性があるファイルには、実 ID や実 URL を含めない。

### 2.6 Zero Trust

外部入力はすべて不信データとして扱う。

対象例:

- ユーザー入力
- フォーム送信値
- Spreadsheet 値
- API 応答
- ファイル
- Script Properties

サーバーサイドで型、長さ、形式、範囲を検証する。
許可リスト方式を優先する。

### 2.7 既存の重要コード・URLの削除禁止

既存の URL、API コード、Webhook、外部連携設定、デプロイ設定を AI の独断で削除・変更してはならない。

変更が必要な場合は、以下を満たすこと。

1. ユーザーから明確な指示がある。
2. 影響範囲を説明する。
3. ロールバック手段を説明する。
4. 変更前の状態を記録する。

## 3. デプロイと本番環境の統制

本番環境は常に稼働している前提で扱う。
本番停止を許容する作業は行わない。

### 3.1 開発中の本番リデプロイ禁止

開発フェーズでは、本番環境へのリデプロイを行わない。
ユーザーが明示的に本番反映を承認した場合のみ、定められた手順で進める。

### 3.2 デプロイ前の絶対確認

テスト環境、本番環境を問わず、デプロイ前に必ず以下を提示する。

- 対象 deployment
- 対象 version
- 影響範囲
- 本番 URL へ影響するか
- ロールバック手段
- 検証方法

ユーザーの明示的な承認なしにデプロイしてはならない。

### 3.3 本番バージョンの固定と記録

本番 deployment の version は常に固定して扱う。
作業開始時と完了時に、本番 deployment ID と version を確認する。

本番 version は以下のいずれかへ必ず記録する。

- `CURRENT_STATE.md`
- `SESSION_HANDOFF.md`
- `TASKS.md`
- `DEPLOYMENT.md`
- 専用の handover / changelog ファイル

### 3.4 開発再開時の確認

キャッチアップ、開発再開、デプロイ作業の開始時は、最初に以下を確認する。

1. 保存された本番 deployment ID
2. 保存された本番 version
3. 実際の `clasp deployments` の状態
4. ローカル HEAD / Apps Script HEAD / deployment version の関係

整合しない場合は作業を止め、ユーザーへ報告する。

## 4. 推測の排除とコンテキスト認識

AI はプロジェクト固有の仕様を推測で補完してはならない。

### 4.1 不明瞭な要件への質問義務

ユーザー指示、既存仕様、運用ルールが不明瞭な場合は、作業を停止し質問する。
独自解釈で実装、削除、デプロイ、設定変更を行ってはならない。

### 4.2 捏造禁止

- 存在しないファイルを読んだと報告してはならない。
- 実行していないコマンドを実行済みと報告してはならない。
- 未確認の仕様を確定事項として書いてはならない。

### 4.3 未知仕様の補完禁止

以下を推測で書かない。

- URL
- ID
- 権限
- ビジネス要件
- シート構造
- 運用フロー
- ユーザー、組織、アカウント情報

必要な場合は、`TBD`、`未確認`、`要確認` と明記する。

### 4.4 ファイル作成ルール

必須または推奨ファイルが存在しない場合、具体的内容を捏造して作成しない。
初期テンプレートとして作成する場合は、未確認箇所を明示する。

既存ファイルを独断で全面上書きしない。
大きく刷新する場合は、目的と影響範囲を説明する。

### 4.5 コードとドキュメントの乖離

ドキュメントと実コードが乖離している場合は、実動しているコードと安全側の判断を優先する。
乖離をユーザーへ報告し、差分修正を基本とする。

## 5. 環境のクリーンアップ

不要なコード、設定、ファイルは事故の原因になる。
ただし、削除は安全性を確認してから行う。

### 5.1 テストコード・一時設定の削除

動作確認に使った以下は、使用後すみやかに削除または無効化する。

- 一時テストコード
- 検証用スクリプト
- 一時 Script Properties
- 一時 Spreadsheet
- 一時 Calendar
- 一時 Drive folder
- 一時 deployment

削除が本番へ影響する可能性がある場合は、実行前にユーザー承認を得る。

### 5.2 キャッチアップ時の整理

開発再開時に、不要ファイル、デッドコード、古い検証物を見つけた場合は、
勝手に削除せず、整理案としてユーザーへ報告する。

### 5.3 クリーンなコミット

コミット前に必ず以下を確認する。

- `git diff`
- `git status`
- `.gitignore`
- 機密情報スキャン
- 自動生成物の混入有無
- 一時ファイルの混入有無

## 6. 作業開始前と完了時の報告

### 6.1 作業開始前

作業前に以下を確認する。

- 目的
- 影響範囲
- 既存仕様との衝突有無
- セキュリティリスク
- 本番環境への影響有無
- ロールバック可否

不明点があれば、実行前に質問する。

### 6.2 虚偽報告禁止

実行していないテスト、コマンド、確認作業を「実行済み」と報告してはならない。
未検証の場合は、未検証であることと理由を明記する。

### 6.3 完了報告フォーマット

作業完了時は、以下を簡潔に報告する。

- 実施内容
- 新規作成ファイル
- 更新ファイル
- 整理・削除したファイル
- 検証結果
- 未検証項目と理由
- 残課題
- 次に必要な操作

## 7. 本リポジトリ固有ルール

### 7.1 プロジェクト構成

このリポジトリは、`clasp` で管理するコンテナバインド Google Apps Script プロジェクトである。
`.clasp.json` の `rootDir` が空文字のため、Apps Script ファイルはリポジトリ直下に置く。

主なファイル:

- `code.js`: Web エントリーポイント、Spreadsheet 書き込み、Calendar / Meet 連携
- `domain.js`: GAS と Node.js テストで共有する純粋ロジック
- `dry_run.js`: GAS 互換 dry-run アダプタ、`runDryTestSuite()`
- `index.html`: Vue.js フォーム UI
- `integration_test.js`: 非本番統合テスト支援
- `チャット通知Bot.js`: Google Chat 通知
- `appsscript.json`: Apps Script manifest
- `architecture.html`: 仕様参照用 HTML
- `tests/`: Node.js ローカルテスト。`clasp push` 対象外
- `scripts/`: 運用スクリプト。`clasp push` 対象外
- `ops/`: deployment policy。`clasp push` 対象外
- `output/`: 生成証跡。コミット、push しない
- `.playwright-mcp/`, `.playwright-cli/`: ブラウザ検査生成物。コミット、push しない

レガシーまたは実験的実装:

- `ヤススさん*`
- `のぐっちさん*`
- `無題.js`
- `仕様確認.js`

これらは明示指示がない限り変更しない。

### 7.2 標準コマンド

サーバー上の Apps Script 最新状態を取得する。

```bash
clasp pull
```

Apps Script へローカル変更を送る。

```bash
clasp push
clasp status
```

ローカルテストを実行する。

```bash
node --test tests/*.test.mjs
```

deployment preflight を実行する。

```bash
node scripts/check-deployment.mjs
```

`architecture.html` をローカル確認する。

```bash
python3 -m http.server 8765
```

### 7.3 TDD と検証

実装変更は Red-Green-Refactor を基本とする。

優先順:

1. Node.js unit test
2. GAS dry-run
3. 非本番 Google リソースでの integration test
4. Playwright による UI 確認
5. 本番 smoke test

本番 smoke test は、ユーザーが本番反映を明示承認した場合のみ実行する。
テスト証跡には個人情報、秘密値、生ログを含めない。

### 7.4 Script Properties

実行時設定は Script Properties へ保存する。
コードや GitHub へ実値を書かない。

主なキー:

- `GAOSYS_SPREADSHEET_ID`
- `GAOSYS_SPREADSHEET_ID_OVERRIDE`
- `CHAT_WEBHOOK_URL`
- `EVALUATION_SHEET_FOLDER_ID`
- `LOGO_IMAGE_URL`
- `SUPPORT_STATUS_SPREADSHEET_URL`
- `TEST_SPREADSHEET_ID`
- `TEST_CALENDAR_ID`
- `TEST_EVALUATION_FOLDER_ID`
- `TEST_CHAT_WEBHOOK_URL`
- `TEST_MAIL_RECIPIENT`

キー名はドキュメントに書いてよい。
実値は書かない。

### 7.5 GitHub へ上げないもの

以下は GitHub へ push しない。

- `.clasp.json`
- `.clasprc.json`
- `.local/`
- OAuth client secret
- token
- Webhook URL
- 実 ID を含む一時ファイル
- `output/`
- `.playwright-mcp/`
- `.playwright-cli/`
- raw logs

### 7.6 デプロイ時の注意

`clasp deploy --versionNumber <version>` は新しい別 URL の deployment を作る。
既存本番 URL を更新するには、必ず対象 deployment ID を指定する。

```bash
clasp deploy --deploymentId <production_deployment_id> --versionNumber <version>
```

この操作は本番反映である。
ユーザーの明示承認なしに実行してはならない。

### 7.7 現在の本番基準

本番 deployment ID と version は、作業開始時に必ず `clasp deployments` で確認する。
保存済み情報と異なる場合は、作業を止めてユーザーへ確認する。

現時点でユーザーが本番 URL として提示した deployment:

```text
ローカル専用ファイル `.local/OPERATIONS.md` を参照する。
```

現時点で記録されている本番 version:

```text
ローカル専用ファイル `.local/OPERATIONS.md` を参照する。
```

この情報は作業再開時に必ず再確認する。
