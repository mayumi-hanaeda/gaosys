# Statement of Work (SOW)

Updated: 2026-08-08
Source of truth: Google Docs 正本「タダスク講師を始めてみたい人の申込みフォーム・仕様書」の
`作業範囲記述書（SOW）`タブ。本ファイルはその派生であり、Google Docs側と乖離した場合は
Google Docs側を正として明示的にマージする。

## 1. プロジェクト概要

- プロジェクト名: タダスク講師申込みフォーム 追加機能開発
- 目的: 既存の「タダスク講師申込みフォーム」システムにおいて、申し込み完了時のバックエンド処理（GAS）を拡張し、評価項目チェックシートの自動発行、管理シート（indexシート）への自動転記、および申込者への自動返信メール送信を実現する。これにより、運営側の手作業を削減し、新講師のスムーズなオンボーディングを支援する。
- 対象システム: Google Apps Script (GAS)、Googleスプレッドシート、Googleカレンダーを利用したWebアプリケーション。

## 2. 作業スコープ（対象範囲）

開発担当者（プログラマー）が対応する作業範囲。フロントエンド（HTML/Vue.js）の改修は含まれず、主にバックエンド（`code.js`等）の機能追加となる。

### 2.1 実装対象機能（In Scope）

1. 評価項目チェックシートの自動生成機能
   - 指定されたテンプレートファイル（スプレッドシート）をコピーし、申込者専用のファイルを生成する。
   - ファイル名を `★【[タダカヨネーム]】評価項目チェックシート` とする。
   - 生成したファイルに対して、申込者のGoogleアカウント（メールアドレス）に編集権限を付与する。
   - 生成したファイルのURLを取得する。
2. indexシートへのデータ自動転記機能
   - スプレッドシート内の `index` シートの最下行に新規行を追加する。
   - B列に申込者の「タダカヨネーム」、F列に上記1で生成した評価シートのURL（リッチテキストリンク）を書き込む。
3. チャットボット通知の拡張
   - 既存のGoogle Chatへの通知処理を改修し、通知メッセージ内に上記1で生成した評価シートのURLを含める。
4. 自動返信メール送信機能
   - 申し込み完了時、申込者のメールアドレス宛に自動返信メールを送信する。
   - メールの件名および本文のテンプレートは、コード内にハードコーディングせず、「設定用」シート等から動的に読み込む仕様とする。
   - テンプレート内の変数（タダカヨネーム、評価シートURL、回答控え）を実際のデータに置換して送信する。

### 2.2 対象外作業（Out of Scope）

- フロントエンド（UI/UX、入力フォームのデザインや項目）の追加・変更。
- 既存のカレンダー連携ロジック（CalendarHandler）の根本的な仕様変更。
- 既存シート（申込リスト、名前リスト、設定用等）の大幅な列構成の変更（メールテンプレート追加用の設定欄拡張は除く）。
- GASの実行制限（6分超過）やAPI上限を回避するための大規模な非同期処理化（本システムは小規模利用を前提とするため）。

## 3. 前提条件と制約事項

- 実行環境: 本システムはデプロイ担当者（`USER_DEPLOYING`）の権限で動作する。Calendar、Drive、Mailの操作もデプロイ担当者の権限で行う（詳細は`SPEC.md` 2.1節）。
- 定数管理: テンプレートファイルのIDなど、環境依存の値はスプレッドシート（設定用シート）またはScript Propertiesから取得し、ハードコーディングを避けること。
- フェイルセーフ: ファイルコピーやメール送信などの各処理ステップにおいて、致命的なエラーが発生した際に原因究明ができるよう、適切なtry-catchとログ出力を実装すること。個人情報・秘密情報はログへ出力しない。

## 4. 成果物

- 改修済みのソースコード: 機能追加が行われたGASのコード。
- 設定・運用手順の引き継ぎ事項: `SPEC.md`、`DEPLOYMENT.md`、`RUNBOOK.md`等。

## 5. 検収条件

実際にフォームから申し込みテストを行い、以下の全ステップが正常に完了すること。

1. カレンダーに予定が登録/更新されること（既存機能の担保）。
2. 評価項目チェックシートが正しいファイル名でコピーされ、申込者に編集権限が付与されること。
3. `index`シートのB列とF列に正しい情報が転記されること。
4. Google Chatに評価シートのURLを含む通知が届くこと。
5. 申込者のメールアドレスに、置換タグが正しく反映された自動返信メールが届くこと。
