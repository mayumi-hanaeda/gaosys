# Claude Code Entry Point

重要:

このリポジトリで Claude Code を使って作業する場合も、最初に必ず `C:\GAOSYS２\gaosys-github\AGENTS.md` を読むこと。

GAOSYS 作業の大元・大原則は `C:\GAOSYS２\gaosys-github\AGENTS.md` である。
`AGENTS.md` こそが、共同編集者全員にとって唯一無二の共通ルールブックであり、最優先のグランドルールである。

この `CLAUDE.md` は Claude Code 用の入口にすぎない。ルールの正本ではない。
本ファイル、他のメモ、引き継ぎ文書、旧作業ディレクトリの内容と `AGENTS.md` が矛盾する場合は、ユーザーの明示指示がない限り `AGENTS.md` を優先する。

作業ディレクトリは `C:\GAOSYS２\gaosys-github` を基準とする。
本番反映、デプロイ、push、設定変更、権限変更、外部通信、機密情報に関わる操作は、`AGENTS.md` の制約に従うこと。
