# Requirements Document (RD)

Updated: 2026-08-08
Source of truth: Google Docs 正本「タダスク講師を始めてみたい人の申込みフォーム・仕様書」の
`要件定義書（RD）`タブ。本ファイルはその派生であり、Google Docs側と乖離した場合は
Google Docs側を正として明示的にマージする。

## 1. プロジェクト概要

- システム名: タダスク講師申込みフォーム
- 開発目的: 既存の申し込み受付およびカレンダー連携機能に加え、新講師のオンボーディングに必要な「評価シートの発行」「管理台帳への転記」「個別メール案内」を自動化し、運営サポーターの事務負担を削減する。
- ターゲットユーザー:
  - 申込者（既存のタダメン）
  - 運営・サポーター（みんなでスキルアップし隊メンバー）

## 2. 業務フロー（To-Be）

申し込み完了時のシステムの振る舞い。

1. 【既存】 ユーザーがフォームから申し込みデータを送信。
2. 【既存】 システムがカレンダーの空き枠を確認し、予定を新規作成または更新（ゲスト追加）。
3. 【既存】 システムが `申込リスト` シートに受付データを記録。
4. 【新規】 システムが `★【タダカヨネーム】評価項目チェックシート` をコピーし、申込者個人の名前を冠した評価シートを作成。申込者に編集権限を付与する。
5. 【新規】 システムが `index` シートの最後尾に、申込者の名前と生成した評価シートのURLを転記。
6. 【新規/改修】 システムがGoogle Chat（みんなでスキルアップし隊スペース）へ通知を送信。その際、生成した評価シートのURLを併記する。
7. 【新規】 システムが、申込者のメールアドレス宛に、今後の流れと評価シートのURLを記載した「自動返信メール」を送信する。

## 3. アクターと権限要件

| アクター | 役割 | 必要な権限・システム上の制約 |
| --- | --- | --- |
| 申込者 | フォームから申し込みを行う。生成された評価シートを入力する。 | 生成された評価シートの「編集権限」を持つ（スクリプトはデプロイ担当者権限で実行されるため、申込者自身はスクリプトの実行ユーザーにはならない）。 |
| 運営・サポーター | 新講師のサポートを行う。通知を受け取り、状況を確認する。 | 各種スプレッドシート（設定用、index等）、テンプレートファイルに対する「閲覧・編集権限」を持つ。 |
| システム（GAS） | デプロイ担当者（`USER_DEPLOYING`）のアカウント権限で各API（Drive, Gmail, Calendar, Sheets）を操作する。 | 各種操作対象リソースへのアクセス権限（初回実行時のスコープ承認済み）を持つ。 |

## 4. 機能要件

### 4.1 F-01: 評価項目チェックシート自動発行機能

- 入力・トリガー: フォームデータの正常な保存完了後。
- 処理内容:
  - テンプレートファイル（IDをシステム設定から取得）を、共有フォルダ（`EVALUATION_SHEET_FOLDER_ID`）へコピーする。
  - ファイル名を `★【タダカヨネーム】評価項目チェックシート` と設定する。
  - 生成したファイルに対し、申込者のメールアドレスを「編集者」として追加する。同一`submissionId`での再実行時は権限を再確認・再付与する。
  - 生成されたファイルのアクセスURLを取得する。
- 出力: 評価シートのURL文字列。
- 例外処理: テンプレートファイルが見つからない、またはドライブ容量不足でコピーできない場合は、`EVALUATION_COPY_FAILED`として記録し、後続処理（シート転記、メール送信）はskippedとする。Chat通知には「発行失敗・要手動対応」というプレースホルダで継続する。

### 4.2 F-02: 管理台帳（indexシート）自動転記機能

- 入力・トリガー: F-01の完了後（評価シートURLが取得できた場合のみ）。
- 処理内容:
  - `LockService`内で`index`シートのB列の最初の空行を取得する。
  - B列に申込者のタダカヨネーム、F列にF-01で取得した評価シートへのリッチテキストリンクを書き込む。
  - A列（既存のArrayFormula）、C〜E列は変更しない。
- 出力: スプレッドシートへの行追加。
- 例外処理: `index`シートが存在しない場合は`INDEX_WRITE_FAILED`として記録し、処理をスキップする。

### 4.3 F-03: チャット通知拡張機能

- 入力・トリガー: 申込受付の各処理完了後。
- 処理内容: 通知本文に申込内容と評価シートの状態（URLまたは失敗プレースホルダ）を含める。
- 出力: Google ChatへのWebhookリクエスト送信。

### 4.4 F-04: 自動返信メール送信機能

- 入力・トリガー: 評価シート発行・権限付与が成功した場合のみ。
- 処理内容:
  - スプレッドシートの「設定用」シートから「件名」と「本文テンプレート」を取得する（ハードコーディング不可）。
  - 本文テンプレート内の `{{tadasukeName}}`、`{{evaluationSheetUrl}}`、`{{responseSummary}}` 等のタグを実データに置換する。
  - 申込者のメールアドレス宛にメールを送信する。
- 出力: メールの送信。
- 例外処理: 送信に失敗した場合は`MAIL_FAILED`として記録する（フロントエンドには全体の「申し込み成功」として返すため、ユーザーの画面遷移はブロックしない）。

## 5. 非機能要件

### 5.1 パフォーマンスとスケーラビリティ

本システムは1日に数件程度の利用を想定する小規模システムであるため、GASの6分間実行タイムアウト制限や、Google Workspaceの1日のAPIリクエスト上限に抵触するリスクは低く、これらを回避するための複雑な非同期化やキューイング処理は実装しない。

### 5.2 可用性とエラーハンドリング（フェイルセーフ）

- 段階的エラー処理: 「シートコピー」「台帳転記」「メール送信」の各処理は独立して実行される。例えば「メール送信」でエラーが発生しても、既に完了した「カレンダー登録」や「シートコピー」をロールバックしない。
- ロギング: 各機能の完了時、および例外発生時には構造化ログを出力する。どの申込（`submissionId`）の処理で、どのステップが失敗したかを記録すること。メールアドレス等の個人情報や秘密値はログへ出力しない。

### 5.3 保守性・運用要件

- 設定値の外部化: メールテンプレート文章や評価シートテンプレートIDは、プログラムコード内に埋め込まず、管理者がスプレッドシート／Script Propertiesから変更できるように設計すること。
- データ保存先: 生成される評価シートは、Script Property `EVALUATION_SHEET_FOLDER_ID` が指す共有フォルダ直下に作成される。運営側は「indexシートからURLを踏んでアクセスする」ことを前提とする。
